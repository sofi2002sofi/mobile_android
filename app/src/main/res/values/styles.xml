<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ToolboxTitle" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:color">@color/white</item>
        <item name="android:textSize">@dimen/default_text_size</item>
    </style>

    <style name="AppInputTextLayout" parent="Widget.Design.TextInputLayout">
        <item name="android:textColorHint">@color/gray</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorAccent">@color/aqua</item>
        <item name="colorControlNormal">@color/aqua</item>
        <item name="colorControlActivated">@color/aqua</item>
    </style>

    <style name="AppInputText" parent="Theme.MaterialComponents.Light">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/default_text_size</item>
    </style>

    <style name="AppButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/default_text_size</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>